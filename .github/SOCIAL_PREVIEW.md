# Social preview image

This repository does not yet have a social preview image set. This is
a GitHub repository setting, not a file, so it can't be automated
from here:

Settings → General → Social preview → Edit → upload an image.

Leaving this note so it doesn't get forgotten.
